fastapi==0.119.1
uvicorn
python-multipart

# Agent + retrieval
langchain==1.0.2
langchain_chroma==1.0.0
langchain_community==0.4
langchain_core==1.0.0
langchain_openai==1.0.1
langchain-classic
langchain-text-splitters
langgraph==1.0.1
langgraph-checkpoint-mongodb
rank_bm25
nltk

# PDF ingestion for the gazette / tax code / SEMA corpus
pypdf

pydantic
pymongo
python-dotenv
requests
httpx
aiohttp

# Audio: pydub joins the per-sentence MP3s from the TTS service.
pydub

# Pillow re-encodes uploaded photos to destroy EXIF/GPS metadata. Without it,
# anonymiser.strip_image_metadata refuses to mark an image as stripped and the
# image is never forwarded — so this is a hard requirement, not an optional one.
Pillow

# IANA timezone database. python:3.10-slim ships without system tzdata, so
# ZoneInfo("Africa/Lagos") would raise.
tzdata
